# ADR 0001：采用单一 pnpm monorepo

- 状态：已接受
- 日期：2026-08-04

## 背景

前端、后端、API 契约、领域规则和课程内容由同一开发流程维护，并需要同步版本和统一验证。

## 决策

使用 pnpm workspace 单仓库，应用放入 `apps/`，共享代码放入 `packages/`。

## 结果

跨层修改可在一个变更中完成并统一测试；仓库规模扩大后需要维持明确包边界，避免任意跨包依赖。
